<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mapper.SetmealAndDishMapper">
    <insert id="insertBatch">
        insert into setmeal_dish(setmeal_id, dish_id, name, price, copies) values
        <foreach collection="list" item="item" separator=",">
            (#{item.setmealId}, #{item.dishId}, #{item.name}, #{item.price}, #{item.copies})
        </foreach>
    </insert>
    <delete id="deleteBatch">
        delete from setmeal_dish where id in
        <foreach collection="list" item="item" separator="," open="(" close=")">
            #{item}
        </foreach>
    </delete>

    <select id="selectCountByDishId" resultType="java.lang.Long" parameterType="java.util.List">
        select count(id) from setmeal_dish where dish_id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </select>
    <select id="selectDishById" resultType="com.vo.DishItemVO">
        select d.name,d.image, d.description, sd.copies from setmeal_dish sd left join dish d on sd.dish_id = d.id
        where sd.setmeal_id = #{id}
    </select>
</mapper>
